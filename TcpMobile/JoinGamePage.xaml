<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TcpMobile.JoinGamePage">
    <Grid RowSpacing="0" ColumnSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        
        <ScrollView>
            <StackLayout
                Grid.Row="0"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand">
                <ListView x:Name="hostsView">

                </ListView>
            </StackLayout>
        </ScrollView>

        <StackLayout
            x:Name="buttonsLayout"
            Grid.Row="1"
            HorizontalOptions="Center"
            VerticalOptions="Center">
            <Button Text="Refresh hosts" HorizontalOptions="Center" VerticalOptions="Center" Clicked="RefreshHosts"></Button>
            <Button Text="Connect" HorizontalOptions="Center" VerticalOptions="Center" Clicked="Connect"></Button>
        </StackLayout>
    </Grid>
</ContentPage>